# Backend (backend/.env or set in Render dashboard)
JWT_SECRET=change-this-to-a-random-secret-string
GEMINI_API_KEY=your-gemini-api-key-here
NEWS_API_KEY=your-newsdata-io-key-here
DATA_DIR=../data
DB_PATH=./swipe2export.db

# Frontend (only needed if frontend and backend are on different origins)
VITE_API_URL=http://localhost:8000
